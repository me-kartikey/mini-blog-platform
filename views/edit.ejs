<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Post | SocialHub</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen flex items-center justify-center">
  <div class="w-full max-w-xl bg-zinc-800 rounded-lg p-8">
    <h2 class="text-2xl font-bold mb-4">Edit your post</h2>
    <form action="/update/<%= post._id %>" method="post">
      <textarea name="content" class="w-full bg-zinc-700 border border-zinc-600 p-4 rounded-md text-white outline-none" rows="4"><%= post.content %></textarea>
      <button class="mt-4 bg-yellow-500 hover:bg-yellow-600 text-white px-4 py-2 rounded-md">Update</button>
    </form>
  </div>
</body>
</html>
